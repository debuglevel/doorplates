version: '3.1'

services:
  rest:
    build: .
    image: doorplates
    ports:
      - "8080:8080"
    environment:
      - INKSCAPE_URL=http://inkscape:8080
  inkscape:
    image: registry.gitlab.com/debuglevel/inkscape-converter-microservice:0-0-1